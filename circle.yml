machine:
  node:
    version: 7.2.1
  environment:
    FOO_BAR: baz
    BROWSER: chrome

dependencies:
  cache_directories:
    - "node_modules"
    - "~/.npm"

  override:
    # Install and cache NPM dependencies
    - npm install


test:
  override:
    - npm run test:ci
